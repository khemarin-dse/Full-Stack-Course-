-- FinTrack Database Setup
-- Run this in MySQL/phpMyAdmin before running Laravel migrations

CREATE DATABASE IF NOT EXISTS fintrack CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE fintrack;

-- After running this file, run these commands in your terminal:
-- cd backend
-- php artisan key:generate
-- php artisan migrate
-- php artisan db:seed
